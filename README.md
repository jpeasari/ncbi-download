# ncbi-download
Bash script to download NCBI data

# Installing `ncbi-genome-download` via Conda

You can install the `ncbi-genome-download` tool from Bioconda using the following command:

```bash
conda install bioconda::ncbi-genome-download

##Verify the installation:

